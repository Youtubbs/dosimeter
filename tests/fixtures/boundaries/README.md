Sample modules used by the architecture boundary test.

They carry a .py.txt extension so that they are read as text and never imported, linted or collected as tests. Each one is a boundary crossing the scanner must reject (or, in the good sample, must accept).
