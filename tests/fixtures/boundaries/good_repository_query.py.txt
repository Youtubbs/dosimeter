"""A repository query through SQLAlchemy, which the scanner must accept."""

from sqlalchemy import select

from dosimeter.repository import orm


def load_exposure(session, exposure_id: str):
    return session.scalars(
        select(orm.ExposureRow).where(orm.ExposureRow.id == exposure_id)
    ).first()
